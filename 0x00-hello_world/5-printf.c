#include <stdio.h>

/**
 * main - Prints a partial quote/line
 *
 * Description: Prints a partial quote/line of some sort
 *
 * Return: 0 if successful. Non-zero otherwise.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
